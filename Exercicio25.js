const config = {
  tema: "dark",
  idioma: "pt-BR",
  versao: "1.0",
  debug: true
};

const {
  tema,
  idioma
} = config;

const novoConfig = {
  tema,
  idioma
};

console.log(novoConfig);